module.exports = {
    theme: {
        extend: {
            colors: {
                // Brand Colors
                // Primary
                bg: "var(--bg)",
                fg: "var(--fg)",
                // Accent
                barRed: "var(--bar-red)",
                barAmber: "var(--bar-amber)",
                barTeal: "var(--bar-teal)",
                barPuple: "var(--bar-purple)",
            }
        }
    }
};
