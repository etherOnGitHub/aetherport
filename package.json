{
    "private": true,
    "workspaces": [
        "apps/*",
        "packages/*"
    ],
    "packageManager": "npm@10.8.1",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev"
    },
    "devDependencies": {
        "baseline-browser-mapping": "^2.8.32",
        "turbo": "^2.6.3"
    }
}
